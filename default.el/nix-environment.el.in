;; If you want to set up paths for binaries, you can setq the values
;; in here. The paths will be completed by nix.

;; You can get the full path by interpolating with the at-symbol

;; N.B; the substituteAll command doesn't like hyphens, replace with
;; underscores

(setq
 emacsql-sqlite3-executable "@sqlite@/bin/sqlite3")
